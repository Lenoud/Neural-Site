---
interface Props {
  backlinks: { slug: string; title: string }[];
}
const { backlinks } = Astro.props;
---

{backlinks.length > 0 && (
  <section>
    <h4>反向链接</h4>
    <ul class="backlink-list">
      {backlinks.map(bl => (
        <li><a href={`${import.meta.env.BASE_URL}notes/${bl.slug}`}>{bl.title}</a></li>
      ))}
    </ul>
  </section>
)}

<style>
  .backlink-list { list-style: none; }
  .backlink-list a {
    display: block;
    padding: 4px 0;
    color: var(--accent);
    font-size: 13px;
  }
</style>
