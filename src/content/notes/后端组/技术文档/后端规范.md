---
title: 后端规范
order: 1
tags: [后端, 规范]
---
# 后端代码规范

## 目录结构

遵循 Go 标准项目布局：`cmd/`、`internal/`、`pkg/`。

## API 设计

所有接口遵循 [[API规范]]，使用 gRPC + Protobuf 定义。

## 数据库

- 使用 GORM 作为 ORM
- 迁移通过 golang-migrate 管理
- 命名规范：表名蛇形 `user_profile`，字段蛇形 `created_at`

## 日志

统一使用结构化日志（zap），日志级别定义参见 [[架构设计]]。
